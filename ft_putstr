#include <unistd.h>
void ft_putstr(char *s)
{
    write(1, s, 6);
}

int main()
{
    char *h;
    int i=0;
    while(  *h!=0 )
    {
       h[i]+= 1;
       i++;
    }
   
    char *h[i] = "asdfg\0";
    ft_putstr(*h);

    return 0;
}
